//create a function that adds two numbers and alerts the sum

const addTwoNums = (n1, n2) => {
  const sum = n1 + n2;
  alert(sum);
}

addTwoNums(2, 2);

//create a function that multiplys three numbers and console logs the product

const multiThreeNums = (n1, n2, n3) => {
  const multi = n1 * n2 * n3;
  console.log(multi);
}

multiThreeNums(2, 2, 2);

//create a function that divides two numbers and returns the ???

const divideTwoNums = (n1, n2) => {
  const division = n1 / n2;
  return division;
}

console.log(divideTwoNums(1, 2))

